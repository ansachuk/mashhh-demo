import { Field } from "formik";
import { styled } from "styled-components";

const StyledField = styled(Field)``;

const StyledLabel = styled.label``;

const ErrorText = styled.p`
	margin: 0;
	padding: 0;
	color: red;
`;

export { StyledField, StyledLabel, ErrorText };
